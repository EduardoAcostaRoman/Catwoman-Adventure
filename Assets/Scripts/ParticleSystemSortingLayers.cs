using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleSystemSortingLayers : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //gameObject.GetComponent<ParticleSystemRenderer>().sortingLayerID = 19;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
